import React from 'react'
import "../Css/Work.css"

function Work() {
    return (
        <div className='Work'>
            <header className='Work-head'>
                <span>{`<`}</span>work<span>{`>`}</span>
            </header>
            <header className='Work-head'>
                <span>{`</`}</span>work<span>{`>`}</span>
            </header>
        </div>
    )
}

export default Work
